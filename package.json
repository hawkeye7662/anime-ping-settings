{
  "name": "anime-ping-settings",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && for f in netlify/functions/*.mjs; do node --check \"$f\"; done"
  }
}
